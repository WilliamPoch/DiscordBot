/// <reference types="node" />
import { Writable } from 'stream';
import { HeaderFormat } from './header';
export default class Untar extends Writable {
    private header;
    private file;
    private totalRead;
    private recordSize;
    private queue;
    _write(data: Buffer, encoding: string, next: Function): any;
    private slice;
    private send;
    private next;
    files(): AsyncIterableIterator<{
        header: HeaderFormat;
        file: Buffer;
    }>;
}
